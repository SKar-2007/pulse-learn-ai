# Supabase frontend credentials
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-anon-key

# API endpoint for backend routes
# Use the backend origin, not the API prefix, because the app adds /api paths automatically.
VITE_API_URL=http://localhost:3001
